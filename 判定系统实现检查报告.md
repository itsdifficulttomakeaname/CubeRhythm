# 判定系统实现检查报告

## 检查日期
2025-02-14

## 检查范围
对照 REMAINING_TASKS.md 中的"音符的判定规则"板块，检查当前Java实现是否符合规则要求。

---

## 规则要求总览

### 通用规则
- ✅ 左右键点击方式不限
- ✅ 都有EXACT/MISS判定

### 各音符类型规则

| 音符类型 | 点击要求 | JUST判定 | 打击音效 |
|---------|---------|---------|---------|
| TAP | 单击一次 | 有 | 有 |
| DRAG | 到达判定面时准心瞄准即可 | 无 | 有 |
| DOUBLE | 需要点击两次 | 有（任一点击在JUST窗口） | 有 |
| HOLD | 到达判定面时有按键按下即可 | 无 | 无 |
| FLICK | 向目标方向转头45°~135° | 无 | 有 |
| EXECUTION | 在某个时间的一些行为 | 不计入判定 | - |

---

## 实现状态分析

### ✅ TAP 音符 - 符合规则

**规则要求:**
- 单击一次
- 有JUST判定
- 有打击音效

**当前实现:**
- ✅ 通过 `handleClickInput()` 处理单击
- ✅ 支持JUST判定（`JudgmentResult.JUST`）
- ✅ 播放打击音效（line 157: `player.playSound(...)`）

**代码位置:** `InputHandler.java:88-128`

---

### ❌ DRAG 音符 - 不符合规则

**规则要求:**
- 到达判定面时准心瞄准即可（**无需点击**）
- 无JUST判定
- 有打击音效

**当前实现问题:**
- ❌ **错误1**: 当前实现要求点击才能判定（line 126-128），但规则要求自动判定
- ✅ 正确过滤JUST判定（line 166: 检查类型并转换为EXACT）
- ✅ 播放打击音效

**需要修改:**
1. 在 `GameSession.tick()` 中添加DRAG音符的自动判定逻辑
2. 每个tick检查玩家视线是否指向DRAG音符
3. 如果指向且在判定范围内，自动触发判定
4. 使用 `ViewDirectionHelper.isLookingAt()` 进行视线检测

**参考Skript实现:** `judge.sk:7-23` (DRAG音符每tick检查准星指向)

---

### ❌ DOUBLE 音符 - 不完整

**规则要求:**
- 需要点击两次
- 一次点击算MISS（第一次点击后0.5s内没有第二次点击）
- 有JUST判定（任一点击在JUST窗口则判为JUST）
- 有打击音效

**当前实现问题:**
- ⚠️ **不完整**: 有追踪变量（`firstClickTime`, `clickCount`）但没有实现逻辑
- ❌ 缺少双击计时器
- ❌ 缺少0.5秒超时检测
- ❌ 缺少"任一JUST则整体JUST"的逻辑
- ✅ 播放打击音效

**需要实现:**
1. 在 `handleClickInput()` 中检测DOUBLE音符类型
2. 第一次点击时记录时间和判定结果
3. 第二次点击时：
   - 检查是否在0.5秒内
   - 如果超时，判为MISS
   - 如果在时间内，取两次判定中较差的结果（如果任一为JUST则为JUST）
4. 启动0.5秒定时器，超时自动判为MISS

**代码位置:** `InputHandler.java:39-41` (变量已定义但未使用)

---

### ❌ HOLD 音符 - 不符合规则

**规则要求:**
- 到达判定面时有按键按下即可（**无需点击**）
- 无JUST判定
- **无打击音效**

**当前实现问题:**
- ❌ **错误1**: 当前实现要求点击才能判定（line 121-122），但规则要求自动判定
- ✅ 正确过滤JUST判定（line 166）
- ❌ **错误2**: 播放了打击音效（line 157），但规则要求无音效

**需要修改:**
1. 在 `GameSession.tick()` 中添加HOLD音符的自动判定逻辑
2. 每个tick检查是否有按键按下（左键或右键）
3. 如果HOLD音符到达判定面且有按键按下，自动触发判定
4. 在 `processJudgment()` 中添加音符类型检查，HOLD音符不播放音效

**参考Skript实现:** `main.sk:419-652` (HOLD音符到达判定线时检查按键状态)

---

### ⚠️ FLICK 音符 - 不完整

**规则要求:**
- 点击后向目标方向转头45°~135°
- 无JUST判定
- 有打击音效

**当前实现问题:**
- ⚠️ **不完整**: `handleFlickNote()` 存在但逻辑简化（line 140-147）
- ❌ 没有实际检查旋转角度
- ❌ 没有使用 `ViewDirectionHelper.isRotatingInDirection()`
- ✅ 正确过滤JUST判定（line 166）
- ✅ 播放打击音效

**需要实现:**
1. 点击FLICK音符时记录当前yaw角度
2. 启动0.5秒定时器
3. 在定时器结束时检查yaw变化
4. 使用 `ViewDirectionHelper.isRotatingInDirection()` 验证旋转方向和角度
5. 如果旋转正确，判为EXACT；否则判为MISS

**代码位置:** `InputHandler.java:140-147` (需要完善)

---

### ✅ EXECUTION 音符 - 符合规则

**规则要求:**
- 在某个时间的一些行为
- 不计入判定

**当前实现:**
- ✅ 有专门的 `ExecutionHandler` 处理
- ✅ 不计入判定系统
- ✅ 在 `NoteSpawner` 中特殊处理（不创建实体）

**代码位置:** `ExecutionHandler.java`, `NoteSpawner.java`

---

## 判定窗口检查

### ✅ 判定窗口正确

**规则要求:**
- EXACT: ±80ms
- JUST: ±200ms

**当前实现:**
- ✅ `JudgmentResult.EXACT`: 80ms (line 7)
- ✅ `JudgmentResult.JUST`: 200ms (line 8)
- ✅ 分数倍率正确（EXACT: 1.0, JUST: 0.7, MISS: 0.0）

**代码位置:** `JudgmentResult.java:7-9`

---

## 音效系统检查

### ⚠️ 音效系统部分正确

**当前实现:**
```java
// InputHandler.java:155-158
if (judgment != JudgmentResult.MISS) {
    player.playSound(player.getLocation(), org.bukkit.Sound.ENTITY_PLAYER_HURT, 0.5f, 1.5f);
}
```

**问题:**
- ❌ HOLD音符不应该播放音效，但当前实现会播放
- ✅ 其他音符类型正确播放音效

**需要修改:**
```java
if (judgment != JudgmentResult.MISS && noteEntity.getType() != NoteType.HOLD) {
    player.playSound(player.getLocation(), org.bukkit.Sound.ENTITY_PLAYER_HURT, 0.5f, 1.5f);
}
```

---

## JUST判定过滤检查

### ✅ JUST判定过滤正确

**当前实现:**
```java
// InputHandler.java:164-172
if (judgment == JudgmentResult.JUST) {
    NoteType type = noteEntity.getType();
    if (type == NoteType.HOLD || type == NoteType.FLICK || type == NoteType.DRAG) {
        displayText = JudgmentResult.EXACT.getDisplayText();
    } else {
        displayText += " " + judgmentManager.getEarlyLate(timingOffset);
    }
}
```

**分析:**
- ✅ 正确过滤HOLD、FLICK、DRAG的JUST判定
- ✅ TAP和DOUBLE保留JUST判定
- ✅ 显示Early/Late提示

---

## 辅助工具检查

### ✅ ViewDirectionHelper 完整实现

**功能:**
- ✅ `isLookingAt()`: 检查玩家是否看向实体（用于DRAG）
- ✅ `isRotatingInDirection()`: 检查旋转方向和角度（用于FLICK）
- ✅ 角度标准化处理

**代码位置:** `ViewDirectionHelper.java`

---

## 问题总结

### 🔴 严重问题（必须修复）

1. **DRAG音符判定方式错误**
   - 当前：需要点击
   - 应该：自动判定（准心瞄准）
   - 影响：游戏玩法完全错误

2. **HOLD音符判定方式错误**
   - 当前：需要点击
   - 应该：自动判定（按键按下）
   - 影响：游戏玩法完全错误

3. **DOUBLE音符逻辑缺失**
   - 当前：只有变量定义，无实现
   - 应该：完整的双击检测和超时逻辑
   - 影响：DOUBLE音符无法正常游玩

### 🟡 中等问题（应该修复）

4. **HOLD音符音效错误**
   - 当前：播放音效
   - 应该：不播放音效
   - 影响：游戏体验不符合设计

5. **FLICK音符逻辑不完整**
   - 当前：简化实现，不检查旋转
   - 应该：完整的旋转检测
   - 影响：FLICK音符判定不准确

---

## 修复优先级

### P0 - 立即修复（影响核心玩法）
1. DRAG音符自动判定
2. HOLD音符自动判定
3. DOUBLE音符双击逻辑

### P1 - 尽快修复（影响游戏体验）
4. HOLD音符音效移除
5. FLICK音符旋转检测

---

## 修复建议

### 1. 在 GameSession.tick() 中添加自动判定

```java
// 在 tick() 方法的音符更新循环中添加
for (NoteEntity entity : entityManager.getAllEntities()) {
    // ... 现有的位置更新代码 ...

    // 自动判定逻辑
    if (!entity.isHit() && Math.abs(distance - 4) < 1.0) {  // 接近判定线
        if (entity.getType() == NoteType.DRAG) {
            // DRAG: 检查准心是否指向
            if (ViewDirectionHelper.isLookingAt(player, entity.getInteraction(), 50.0)) {
                int timingOffset = judgmentManager.calculateTimingOffset(entity, settings.getSpeed());
                JudgmentResult judgment = judgmentManager.judge(timingOffset);
                // 处理判定...
            }
        } else if (entity.getType() == NoteType.HOLD) {
            // HOLD: 检查是否有按键按下
            if (isAnyKeyPressed(player)) {
                int timingOffset = judgmentManager.calculateTimingOffset(entity, settings.getSpeed());
                JudgmentResult judgment = judgmentManager.judge(timingOffset);
                // 处理判定（不播放音效）...
            }
        }
    }
}
```

### 2. 在 InputHandler 中完善 DOUBLE 逻辑

```java
private void handleClickInput(Player player) {
    // ... 现有的射线检测代码 ...

    if (noteEntity.getType() == NoteType.DOUBLE) {
        handleDoubleNote(noteEntity, judgment, timingOffset);
    } else {
        processJudgment(noteEntity, judgment, timingOffset);
    }
}

private void handleDoubleNote(NoteEntity noteEntity, JudgmentResult judgment, int timingOffset) {
    int currentCount = clickCount.getOrDefault(noteEntity, 0) + 1;
    clickCount.put(noteEntity, currentCount);

    if (currentCount == 1) {
        // 第一次点击
        firstClickTime.put(noteEntity, System.currentTimeMillis());
        // 存储第一次判定结果
        // 启动0.5秒定时器
        PlanetLib.getScheduler().runLater(() -> {
            if (clickCount.getOrDefault(noteEntity, 0) == 1) {
                // 超时，判为MISS
                processJudgment(noteEntity, JudgmentResult.MISS, 0);
                clickCount.remove(noteEntity);
                firstClickTime.remove(noteEntity);
            }
        }, 10L);  // 0.5秒 = 10 ticks
    } else if (currentCount == 2) {
        // 第二次点击
        long elapsed = System.currentTimeMillis() - firstClickTime.get(noteEntity);
        if (elapsed <= 500) {
            // 在时间内，取两次判定中较差的结果
            // 如果任一为JUST，则为JUST
            processJudgment(noteEntity, judgment, timingOffset);
        } else {
            // 超时
            processJudgment(noteEntity, JudgmentResult.MISS, 0);
        }
        clickCount.remove(noteEntity);
        firstClickTime.remove(noteEntity);
    }
}
```

### 3. 修复音效播放逻辑

```java
// 在 processJudgment() 中修改
if (judgment != JudgmentResult.MISS && noteEntity.getType() != NoteType.HOLD) {
    player.playSound(player.getLocation(), org.bukkit.Sound.ENTITY_PLAYER_HURT, 0.5f, 1.5f);
}
```

### 4. 完善 FLICK 判定

```java
private void handleFlickNote(NoteEntity noteEntity, Player player, JudgmentResult judgment) {
    if (judgment != JudgmentResult.MISS) {
        noteEntity.setHit(true);
        float previousYaw = this.previousYaw;

        // 0.5秒后检查旋转
        PlanetLib.getScheduler().runLater(() -> {
            float currentYaw = ViewDirectionHelper.getPlayerYaw(player);
            String targetDirection = noteEntity.getTurn();

            if (ViewDirectionHelper.isRotatingInDirection(player, previousYaw, currentYaw, targetDirection)) {
                processJudgment(noteEntity, JudgmentResult.EXACT, 0);
            } else {
                processJudgment(noteEntity, JudgmentResult.MISS, 0);
            }
        }, 10L);  // 0.5秒
    }
}
```

---

## 结论

当前实现的**判定窗口**和**JUST过滤**是正确的，但**音符判定逻辑**存在严重问题：

- ✅ **正确**: TAP, EXECUTION, 判定窗口, JUST过滤
- ❌ **错误**: DRAG (需要点击 → 应该自动), HOLD (需要点击 → 应该自动), HOLD音效
- ⚠️ **不完整**: DOUBLE (无逻辑), FLICK (简化实现)

**建议立即修复DRAG、HOLD、DOUBLE的判定逻辑，这些是影响核心玩法的严重问题。**

---

*报告生成时间: 2025-02-14*
*检查工具: Claude Code*
